import {
    breakpoint,
    color,
    spacing,
    fontSize,
    fontWeight,
    border,
    borderRadius,
    boxShadow,
  } from "./tokens";
  
  export const theme = {
    breakpoint,
    color,
    spacing,
    fontSize,
    fontWeight,
    border,
    borderRadius,
    boxShadow,
  };